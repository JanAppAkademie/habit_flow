name: habit_flow
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  easy_localization: ^3.0.1

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.0
  hive_ce: ^2.15.1
  hive_ce_flutter: ^2.3.3
  shared_preferences: ^2.5.3
  # uuid and flutter_dotenv removed; using internal helpers instead
  state_notifier: ^1.0.0
  dio: ^5.9.0
  go_router: ^14.8.1
  shimmer: ^3.0.0
  flutter_local_notifications: ^19.5.0
  supabase_flutter: ^2.3.4

  permission_handler: ^11.3.1
  timezone: ^0.10.1
  connectivity_plus: ^7.0.0
  hive: ^2.2.3
  internet_connection_checker: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

  build_runner: ^2.4.14
  hive_ce_generator: ^1.9.0

flutter:
  uses-material-design: true

  assets:
    - assets/langs/de.json
    - assets/supabase/.env
    - assets/motivation/motivation.json